import pandas as pd
import numpy as np

s = pd.Series(4)
deef= pd.Series(data=[1, 2, 3, 4, 5], index=['a', 'b', 'c', 'd', 'e'])
print(deef)
#%%
deef = pd.Series(data=[1, 2, 3, 4, 5], index=['a', 'b', 'c', 'd', 'e'])
print(deef)